/* PrimeNest Properties — single-page build entry.
   The same canonical files that ship as /css/style.css and /js/*.js on GitHub Pages
   are bundled here so the whole site also runs as one self-contained document. */
import "../public/css/style.css";
import "../public/js/properties.js";
import "../public/js/property-details.js";
import "../public/js/main.js";
